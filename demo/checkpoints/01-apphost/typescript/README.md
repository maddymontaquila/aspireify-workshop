# TypeScript AppHost

This TypeScript AppHost orchestrates the shared bingo application source.

Install its local tooling and generate the Aspire SDK:

```bash
npm install
aspire restore
```

Then run the application:

```bash
aspire run
```

Sign in to the admin portal with username `admin` and password `admin`.
